services:
  easymall-frontend:
    image: yunluoxincheng/easymall-frontend:latest
    build: .
    container_name: easymall-frontend
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    restart: unless-stopped
