services:
  easymall-frontend:
    build: .
    container_name: easymall-frontend
    ports:
      - "${FRONTEND_PORT:-80}:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
